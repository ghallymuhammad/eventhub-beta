# Email Service Configuration for Gmail
# You need to generate an App Password for Gmail:
# 1. Go to Google Account settings
# 2. Security > 2-Step Verification (enable if not enabled)
# 3. Security > App passwords > Generate password for "Mail"
# 4. Use the generated 16-character password below

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=muh.ghally@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM_EMAIL=muh.ghally@gmail.com
SMTP_FROM_NAME=EventHub

# Make sure to add these to your actual .env.local file
